// Reconstructed from user-provided shape diagnostics, NOT captured payloads.
// Confirmed: references can be absent; a reference can have no name.
// The ID and entity_type below are synthetic: diagnostics did not expose values.
export const observedReferenceShapes = [
  {},
  { references: [{ id: 9, entity_type: 'synthetic-reference' }] },
];
